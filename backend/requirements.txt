# ============================================================
#  AI Translator Website — Python Backend Requirements
#  Install with:
#    .venv\Scripts\pip.exe install -r backend\requirements.txt
# ============================================================

# --- Web Framework ---
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# --- Database ---
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
aiosqlite==0.20.0

# --- Data Validation ---
pydantic==2.8.2
pydantic-settings==2.3.4
email-validator==2.2.0

# --- Google Gemini AI ---
google-generativeai==0.7.2

# --- Authentication ---
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
google-auth==2.31.0

# --- PDF Processing ---
pymupdf==1.24.7

# --- OCR for Image Translation (optional but recommended) ---
# WARNING: paddlepaddle is ~500 MB. Comment these out if you only need text/PDF translation.
paddleocr==2.8.1
paddlepaddle==3.0.0
Pillow==10.4.0

# --- Image Pre-processing (used by OCR pipeline) ---
opencv-python-headless==4.10.0.84
numpy==1.26.4

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.0
